<Window x:Class="MultividStreamer.App.Selection.AddSourcesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ajouter des sources"
        Height="620"
        Width="980"
        MinHeight="460"
        MinWidth="720"
        WindowStartupLocation="CenterOwner">
    <DockPanel Margin="16">
        <Grid DockPanel.Dock="Top" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button x:Name="UpButton"
                    Grid.Column="0"
                    Width="92"
                    Margin="0,0,8,0"
                    Click="UpButton_Click">
                Remonter
            </Button>

            <TextBox x:Name="CurrentPathText"
                     Grid.Column="1"
                     IsReadOnly="True"
                     VerticalContentAlignment="Center" />

            <Button x:Name="AddCurrentFolderButton"
                    Grid.Column="2"
                    Width="160"
                    Margin="8,0,0,0"
                    Click="AddCurrentFolderButton_Click">
                Ajouter dossier courant
            </Button>
        </Grid>

        <Grid DockPanel.Dock="Bottom" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="StatusText"
                       Grid.Column="0"
                       VerticalAlignment="Center"
                       Foreground="#555" />

            <Button Grid.Column="1"
                    Width="150"
                    Margin="0,0,8,0"
                    IsDefault="True"
                    Click="AddSelectionButton_Click">
                Ajouter la selection
            </Button>

            <Button Grid.Column="2"
                    Width="100"
                    IsCancel="True">
                Annuler
            </Button>
        </Grid>

        <ListView x:Name="EntriesList"
                  SelectionMode="Extended"
                  MouseDoubleClick="EntriesList_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Type"
                                    Width="95"
                                    DisplayMemberBinding="{Binding TypeLabel}" />
                    <GridViewColumn Header="Nom"
                                    Width="300"
                                    DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Chemin"
                                    Width="360"
                                    DisplayMemberBinding="{Binding FullPath}" />
                    <GridViewColumn Header="Taille"
                                    Width="90"
                                    DisplayMemberBinding="{Binding SizeLabel}" />
                    <GridViewColumn Header="Modifie"
                                    Width="130"
                                    DisplayMemberBinding="{Binding ModifiedLabel}" />
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</Window>
